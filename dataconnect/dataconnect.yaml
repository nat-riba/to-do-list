specVersion: "v1beta"
serviceId: "to-do-list"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "to-do-list-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
